/**
 * Annotated MCP - Entry Point
 *
 * A structured notepad for Claude's reasoning with audience-annotated responses.
 *
 * CORE PATTERN: Accept-Store-Echo (with dual content blocks)
 * - ACCEPT: Claude provides structured thoughts
 * - STORE: MCP stores them unchanged
 * - ECHO: MCP returns them unchanged + session context
 *   Block 1 (assistant): full structured JSON
 *   Block 2 (user): clean human-readable summary
 *
 * The MCP is a MIRROR. It cannot generate, suggest, enhance, or transform.
 */
export {};
//# sourceMappingURL=index.d.ts.map